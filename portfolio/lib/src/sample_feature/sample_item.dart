import 'package:flutter/material.dart';

/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.title, this.text, this.color, this.img);

  final String title;
  final String text;
  final MaterialColor color;
  final String img;
}
